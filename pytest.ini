[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers --import-mode=importlib -m "not manual and not slow"
markers =
    unit: unit tests
    integration: integration tests
    slow: slow tests
    manual: tests that require an explicitly started external service or operator action
    asyncio: asyncio tests
consider_namespace_packages = true
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:pytest_asyncio
    ignore:.*get_event_loop_policy.*:DeprecationWarning
    ignore:.*set_event_loop_policy.*:DeprecationWarning
