# Dynamic Evidence Register

All evidence below ran in `W:\novel\test` using
`W:\novel\test\.venv\Scripts\python.exe` (Python 3.14.6), synthetic trace
values, and temporary SQLite data. No formal-workspace database was opened or
modified.

| Evidence | Classification | Verified result |
| --- | --- | --- |
| Final provider trace probe | real composition plus final provider spy | All six locked-setting markers are present in both initial and re-dispatched final provider prompts; raw prompt content is not recorded |
| Setting clear probe | real service and Variable Hub path | Empty values replace canonical and compatibility aliases; old values no longer persist |
| Blueprint safety tests | real planning service with controlled repositories | Authored prose blocks destructive replanning; safe macro failure makes no unsafe fallback call; direct next-act checks shared constraints |
| SQLite/migration tests | real migration runner and repositories | Parent updates retain children, natural-key guard is compatible, fresh database order completes, and recovery fields round-trip |
| FastAPI Mock-LLM acceptance | real routes, services, SQLite, deterministic external LLM only | New novel, settings refresh, macro plan, safe structure confirmation, and Beat Sheet persistence/read all succeed |
| StoryPipeline continuity | real default pipeline and aftermath fixture | Memory persists into the next context, injected vector failure recovers, and no artificial direct-memory path is substituted |
| Long process regressions | real 30- and 100-chapter fixtures | 30 chapter recovery passes; both 100-chapter selectors execute true 100-iteration loops and pass |
| Restart/state regression | real runtime, repository, and API projections | Restart reason persists, explicit start clears it, and resume behavior remains durable |
| Frontend build | real TypeScript/Vite production build | Shared config check and production build pass |

## Accurate E2E Classification

- **Real API + Mock LLM:** executed for novel creation through planning,
  structure confirmation, and Beat Sheet persistence.
- **Real StoryPipeline + deterministic test doubles at external boundaries:**
  executed for prose-memory continuity and long-running aftermath behavior.
- **Real UI + real API + Mock LLM full autopilot browser flow:** not executed.
  No report claims that this browser-level workflow has been completed.
- **Real external provider:** not executed; deterministic providers prevent
  cost, credential, and user-data exposure during acceptance.

The older direct planning-service script is not promoted as final E2E
evidence because interpreter shutdown can block outside pytest isolation. The
project test environment is the reliable evidence path. See
`20-e2e-acceptance.md` for the requirement-by-requirement scope.
